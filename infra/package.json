{
  "name": "infra",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "NODE_OPTIONS=\"--require ./force-ipv4.cjs\" cdk",
    "synth": "npm run cdk -- synth",
    "diff": "npm run cdk -- diff",
    "deploy": "npm run cdk -- deploy",
    "destroy": "npm run cdk -- destroy"
  },
  "devDependencies": {
    "@swc/core": "^1.15.0",
    "@swc/jest": "^0.2.39",
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "jest": "^30",
    "aws-cdk": "2.1132.0",
    "tsx": "^4.23.0",
    "typescript": "~7.0.2"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.5.0"
  }
}
